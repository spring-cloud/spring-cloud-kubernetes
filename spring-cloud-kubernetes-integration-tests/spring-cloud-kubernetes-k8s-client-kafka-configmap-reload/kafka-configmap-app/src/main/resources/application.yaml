spring:
  application:
    name: app
  cloud:
    bus:
      refresh:
        enabled: false
      enabled: true
      destination: app
    stream:
      default-binder: kafka
management:
  endpoint:
    health:
      probes:
        enabled: true
server:
  port: 8081
