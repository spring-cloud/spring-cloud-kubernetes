spring:
  application:
    name: fabric8-discovery-with-bootstrap
  cloud:
    config:
      discovery:
        enabled: true
      fail-fast: true
      retry:
        max-attempts: 60
      enabled: true
      request-connect-timeout: 1000
      request-read-timeout: 1000
logging:
  level:
    root: DEBUG
