apiVersion: v1
kind: Service
metadata:
  labels:
    app: app-b
  name: app-b
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 32322
  selector:
    app: app-b
  type: NodePort
