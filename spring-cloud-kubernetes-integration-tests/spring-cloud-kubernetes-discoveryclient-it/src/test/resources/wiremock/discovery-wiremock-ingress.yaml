apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: servicea-wiremock-ingress
spec:
  rules:
    - http:
        paths:
          - path: /wiremock
            pathType: Prefix
            backend:
              service:
                name: servicea-wiremock
                port:
                  number: 8080
