spring:
  cloud:
    kubernetes:
      enabled: true
      config:
        enabled: true
      secrets:
        enabled: true
        enable-api: true
      reload:
        enabled: true
        monitoring-secrets: true
logging:
  level:
    org:
      springframework:
        cloud:
          kubernetes: DEBUG
