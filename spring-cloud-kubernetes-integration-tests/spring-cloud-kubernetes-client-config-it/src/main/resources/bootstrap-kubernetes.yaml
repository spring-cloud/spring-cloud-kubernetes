spring:
  cloud:
    kubernetes:
      secrets:
        enable-api: true
      reload:
        enabled: true
        monitoring-secrets: true
logging:
  level:
    org:
      springframework:
        cloud:
          kubernetes: DEBUG
