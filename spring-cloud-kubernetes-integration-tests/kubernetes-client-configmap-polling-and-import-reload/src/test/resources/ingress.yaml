apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kubernetes-client-configmap-ingress-polling-and-import-reload
  namespace: default
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kubernetes-client-configmap-polling-and-import-reload
                port:
                  number: 8080
