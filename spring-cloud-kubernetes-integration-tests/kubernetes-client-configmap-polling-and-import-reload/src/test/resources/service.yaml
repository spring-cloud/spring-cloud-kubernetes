apiVersion: v1
kind: Service
metadata:
  labels:
    app: kubernetes-client-configmap-polling-and-import-reload
  name: kubernetes-client-configmap-polling-and-import-reload
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: kubernetes-client-configmap-polling-and-import-reload
  type: ClusterIP
