apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: it-ingress-app-a
  namespace: default
spec:
  rules:
    - http:
        paths:
          - path: /app-a
            pathType: Prefix
            backend:
              service:
                name: spring-cloud-kubernetes-configuration-watcher-app-a-service
                port:
                  number: 8080
