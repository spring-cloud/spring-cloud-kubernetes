apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: it-ingress-app-b
  namespace: default
spec:
  rules:
    - http:
        paths:
          - path: /app-b
            pathType: Prefix
            backend:
              service:
                name: spring-cloud-kubernetes-configuration-watcher-app-b
                port:
                  number: 8081
