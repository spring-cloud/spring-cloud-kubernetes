spring:
  application:
    name: poll-reload-mount-boostrap
  cloud:
    kubernetes:
      reload:
        enabled: true
        strategy: shutdown
        mode: polling
        period: 5000
        monitoring-secrets: true

