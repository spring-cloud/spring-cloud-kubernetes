spring:
  application:
    name: poll-reload-mount-boostrap

management:
  endpoint:
    refresh:
      access: unrestricted
    restart:
      access: unrestricted
  endpoints:
    web:
      exposure:
        include: '*'
