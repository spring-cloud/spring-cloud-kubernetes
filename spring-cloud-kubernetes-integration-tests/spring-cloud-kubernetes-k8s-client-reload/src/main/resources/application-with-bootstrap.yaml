logging:
  level:
    root: DEBUG

spring:
  application:
    name: poll-reload-mount-boostrap
  config:
    import: "kubernetes:,configtree:/tmp/"

management:
  endpoint:
    refresh:
      access: unrestricted
    restart:
      access: unrestricted
  endpoints:
    web:
      exposure:
        include: '*'
