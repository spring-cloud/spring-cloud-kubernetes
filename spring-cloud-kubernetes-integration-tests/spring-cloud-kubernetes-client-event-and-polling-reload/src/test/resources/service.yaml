apiVersion: v1
kind: Service
metadata:
  labels:
    app: spring-cloud-k8s-client-service-reload
  name: spring-cloud-k8s-client-service-reload
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: spring-cloud-k8s-client-service-reload
  type: ClusterIP
