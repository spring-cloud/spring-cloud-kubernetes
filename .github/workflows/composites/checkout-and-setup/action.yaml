name: checkout and setup project
description: checkout and setup project
runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-java@v2
        with:
          distribution: 'temurin'
          java-version: '17'
