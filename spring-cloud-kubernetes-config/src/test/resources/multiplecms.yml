---
spring:
  application:
    name: name-in-file
  cloud:
    kubernetes:
      reload:
        enabled: false
      config:
        name: defname
        namespace: defnamespace
        retryPolicy:
          maxAttempts: 3
          delay: 500
        sources:
          - name: s1
          - namespace: s2
          - name: othername
            namespace: othernamespace
