spring:
  application:
    name: sanitize
  cloud:
    kubernetes:
      secrets:
        enabled: true
        sources:
          - name: sanitize-secret
            namespaces: test
          - name: sanitize-secret-two
            namespaces: test
      config:
        sources:
          - name: sanitize-configmap
            namespace: test
