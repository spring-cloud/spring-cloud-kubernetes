spring:
  application:
    name: labeled-configmap-with-profile
  cloud:
    kubernetes:
      config:
        enableApi: true
        useNameAsPrefix: true
        namespace: spring-k8s
        sources:
          - labels:
              color: blue
            explicitPrefix: blue
          - labels:
              color: green
          - labels:
              color: red
          - labels:
              color: yellow
            useNameAsPrefix: true
